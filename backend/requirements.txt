# ===== Python 依赖包 =====
# 安装方式：pip install -r requirements.txt

fastapi>=0.100.0           # Web API 框架
uvicorn>=0.23.0            # 服务器
sqlalchemy>=2.0.0          # 数据库操作
pydantic>=2.0.0            # 数据验证
python-multipart>=0.0.6    # 文件上传支持
httpx>=0.25.0              # HTTP 客户端（DeepSeek 调用）
websocket-client>=1.0.0    # WebSocket 客户端（讯飞 IAT v2）
python-dotenv>=1.0.0       # .env 环境变量管理
